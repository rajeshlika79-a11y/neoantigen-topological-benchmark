[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "neoantigen-topological-benchmark"
version = "0.1.0"
description = "A benchmark for neoantigen topological analysis"
requires-python = ">=3.7"
